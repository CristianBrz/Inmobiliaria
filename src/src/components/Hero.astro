---
import { ArrowRight, Search } from 'lucide-react';
---

<div class="relative min-h-[90vh] flex items-center justify-center overflow-hidden bg-gradient-to-br from-blue-950 to-blue-900">
  <!-- Background Image -->
  <div class="absolute inset-0">
    <img
      src="https://images.unsplash.com/photo-1706808849827-7366c098b317?crop=entropy&cs=tinysrgb&fit=max&fm=jpg&ixid=M3w3Nzg4Nzd8MHwxfHNlYXJjaHwxfHxtb2Rlcm4lMjBsdXh1cnklMjBob3VzZSUyMGV4dGVyaW9yfGVufDF8fHx8MTc2MzA0NzEwMnww&ixlib=rb-4.1.0&q=80&w=1080"
      alt="Casa moderna"
      class="w-full h-full object-cover opacity-30"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-blue-950/50 via-blue-900/30 to-blue-950/70"></div>
  </div>

  <!-- Content -->
  <div class="relative container mx-auto px-4 lg:px-8 pt-20 pb-16 text-center">
    <div class="max-w-4xl mx-auto animate-fade-in">
      <div class="inline-block mb-4 px-4 py-2 bg-amber-400/20 rounded-full border border-amber-400/30">
        <span class="text-amber-300 text-sm">Confianza y Profesionalismo</span>
      </div>

      <h1 class="text-4xl md:text-5xl lg:text-6xl text-white mb-6">
        Encontrá tu próximo hogar{' '}
        <span class="text-amber-400">con nosotros</span>
      </h1>

      <p class="text-lg md:text-xl text-gray-200 mb-10 max-w-2xl mx-auto">
        Más de 20 años ayudando a familias a encontrar su hogar ideal. 
        Experiencia, compromiso y atención personalizada.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <button
          data-scroll-to="propiedades"
          class="scroll-button group px-8 py-4 bg-amber-400 text-blue-950 rounded-lg hover:bg-amber-300 transition-all duration-300 flex items-center gap-2 shadow-xl hover:shadow-2xl hover:scale-105"
        >
          <Search className="w-5 h-5" />
          Ver propiedades
          <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
        </button>

        <button
          data-scroll-to="contacto"
          class="scroll-button px-8 py-4 bg-white/10 text-white border-2 border-white/30 rounded-lg hover:bg-white/20 transition-all duration-300 backdrop-blur-sm"
        >
          Contactanos
        </button>
      </div>

      <!-- Floating Stats -->
      <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto">
        <div class="bg-white/10 backdrop-blur-md rounded-lg p-6 border border-white/20">
          <div class="text-3xl text-amber-400 mb-2">500+</div>
          <div class="text-sm text-gray-200">Propiedades</div>
        </div>
        <div class="bg-white/10 backdrop-blur-md rounded-lg p-6 border border-white/20">
          <div class="text-3xl text-amber-400 mb-2">1200+</div>
          <div class="text-sm text-gray-200">Clientes satisfechos</div>
        </div>
        <div class="bg-white/10 backdrop-blur-md rounded-lg p-6 border border-white/20">
          <div class="text-3xl text-amber-400 mb-2">20+</div>
          <div class="text-sm text-gray-200">Años de experiencia</div>
        </div>
        <div class="bg-white/10 backdrop-blur-md rounded-lg p-6 border border-white/20">
          <div class="text-3xl text-amber-400 mb-2">98%</div>
          <div class="text-sm text-gray-200">Recomendación</div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.querySelectorAll('.scroll-button').forEach(button => {
    button.addEventListener('click', (e) => {
      const target = (e.currentTarget as HTMLElement).getAttribute('data-scroll-to');
      if (target) {
        const element = document.getElementById(target);
        if (element) {
          const offset = 80;
          const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
          window.scrollTo({
            top: elementPosition - offset,
            behavior: 'smooth'
          });
        }
      }
    });
  });
</script>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }
</style>
